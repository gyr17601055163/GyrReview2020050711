Redis的应用场景
    1.可以用做数据缓存
    2.可以做幂等性控制
        使用唯一索引（性能比较差）
        token机制，防止页面重复提交（在页面第一次加载的时候，去后台controller取一个token回去，当点击提交按钮提交表单
            的时候，将token一并提交到后台对应的处理方法中取，在后台处理方法中去检查这个token是否存在，如果存在则删除，
            这里存放token的话，可以使用redis）
        分布式锁【reids分布式锁】